# WayaGetSystemData Environment Variables

# API Key for authentication (optional).
# If set, requests must include 'Authorization: Bearer YOUR_API_KEY' header.
# Example: WGS_API_KEY="your_super_secret_api_key_here"
WGS_API_KEY=""

# Port for the server to listen on (optional).
# If not set, defaults to 3000.
# Example: PORT=8080
PORT=
